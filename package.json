{
  "private": true,
  "name": "@metreeca/demo",
  "description": "Model-driven user interfaces for queryable linked data APIs.",
  "keywords": [
    "typescript",
    "preact",
    "json-ld",
    "model-driven",
    "ui"
  ],
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npm run clean --workspaces && rm -rf dist docs node_modules",
    "prime": "npm ci",
    "setup": "npm ci && sh package.sh",
    "build": "npm run build --workspaces && rm -rf docs && npx typedoc",
    "check": "npm run check --workspaces",
    "proof": "npm run build && npx wds --root-dir docs --open",
    "serve": "npm --prefix demo run serve"
  },
  "devDependencies": {
    "@web/dev-server": "^1.0.0",
    "typedoc": "^0.28.20",
    "vitest": "^5.0.1"
  },
  "allowScripts": {
    "fsevents@2.3.3": true
  }
}
